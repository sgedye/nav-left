import React from "react"

const Events = () => {
  return (
    <section className="page-wrapper">
      <div>
        <p>Events Events Events Events Events Events Events Events</p>
        <p>Events Events Events Events Events Events Events Events</p>
        <p>Events Events Events Events Events Events Events Events</p>
      </div>
    </section>
  )
}

export default Events
